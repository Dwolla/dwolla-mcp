/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { DwollaMcpCore } from "../core.js";

/**
 * Creates a new Dwolla MCP client with the provided token for per-request authentication.
 * 
 * @param token - The Dwolla API access token (Bearer token)
 * @param serverURL - Optional server URL (defaults to sandbox)
 * @returns Configured DwollaMcpCore client
 */
export function createDwollaClient(token: string, serverURL?: string): DwollaMcpCore {
  return new DwollaMcpCore({
    security: {
      bearerAuth: token,
    },
    serverURL: serverURL || "https://api-sandbox.dwolla.com",
  });
}