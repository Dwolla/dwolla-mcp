/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customers_fundingSources_list } from "../../funcs/customers_fundingSources_list.js";
import { ListCustomerFundingSourcesRequest$zodSchema } from "../../models/operations/listcustomerfundingsources.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomerFundingSourcesRequest$zodSchema,
};

export const tool$customers_fundingSources_list: ToolDefinition<typeof args> = {
  name: "customers-funding-sources-list",
  description: `List customer funding sources

List customer funding sources`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customers_fundingSources_list(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
