/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPaymentsListForCustomer } from "../../funcs/massPaymentsListForCustomer.js";
import { ListCustomerMassPaymentsRequest$zodSchema } from "../../models/listcustomermasspaymentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomerMassPaymentsRequest$zodSchema,
};

export const tool$massPaymentsListForCustomer: ToolDefinition<typeof args> = {
  name: "mass-payments-list-for-customer",
  description: `List mass payments for customer

List mass payments for customer`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPaymentsListForCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
