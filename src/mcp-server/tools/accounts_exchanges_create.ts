/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accounts_exchanges_create } from "../../funcs/accounts_exchanges_create.js";
import { CreateAccountExchangeRequest$zodSchema } from "../../models/operations/createaccountexchange.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateAccountExchangeRequest$zodSchema,
};

export const tool$accounts_exchanges_create: ToolDefinition<typeof args> = {
  name: "accounts-exchanges-create",
  description: `Create an exchange for an account

Create an exchange for an account. The request body will vary based on the exchange partner.
For Finicity, the request body will include finicity-specific fields.
For MX Secure Exchange, the request body will include a token.
For Flinks Secure Exchange, the request body will include a token.
For Plaid Secure Exchange, the request body will include a token.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accounts_exchanges_create(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
