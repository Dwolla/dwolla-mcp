/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfers_cancel } from "../../funcs/transfers_cancel.js";
import { CancelTransferRequest$zodSchema } from "../../models/operations/canceltransfer.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CancelTransferRequest$zodSchema,
};

export const tool$transfers_cancel: ToolDefinition<typeof args> = {
  name: "transfers-cancel",
  description: `Cancel a transfer

Cancel a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfers_cancel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
