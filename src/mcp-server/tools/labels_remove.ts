/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labels_remove } from "../../funcs/labels_remove.js";
import { RemoveLabelRequest$zodSchema } from "../../models/operations/removelabel.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RemoveLabelRequest$zodSchema,
};

export const tool$labels_remove: ToolDefinition<typeof args> = {
  name: "labels-remove",
  description: `Remove a label

Remove a label`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labels_remove(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
