/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { businessClassifications_get } from "../../funcs/businessClassifications_get.js";
import { RetrieveBusinessClassificationRequest$zodSchema } from "../../models/operations/retrievebusinessclassification.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RetrieveBusinessClassificationRequest$zodSchema,
};

export const tool$businessClassifications_get: ToolDefinition<typeof args> = {
  name: "business-classifications-get",
  description: `Retrieve a business classification

Retrieve a business classification`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await businessClassifications_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
