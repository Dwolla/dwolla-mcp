/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangePartners_get } from "../../funcs/exchangePartners_get.js";
import { GetExchangePartnerRequest$zodSchema } from "../../models/operations/getexchangepartner.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetExchangePartnerRequest$zodSchema,
};

export const tool$exchangePartners_get: ToolDefinition<typeof args> = {
  name: "exchange-partners-get",
  description: `Retrieve exchange partner

Retrieve exchange partner`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exchangePartners_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
