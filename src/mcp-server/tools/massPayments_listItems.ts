/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPayments_listItems } from "../../funcs/massPayments_listItems.js";
import { ListMassPaymentItemsRequest$zodSchema } from "../../models/operations/listmasspaymentitems.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListMassPaymentItemsRequest$zodSchema,
};

export const tool$massPayments_listItems: ToolDefinition<typeof args> = {
  name: "mass-payments-list-items",
  description: `List items for a mass payment

List items for a mass payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPayments_listItems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
