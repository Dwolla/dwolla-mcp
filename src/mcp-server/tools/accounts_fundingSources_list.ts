/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accounts_fundingSources_list } from "../../funcs/accounts_fundingSources_list.js";
import { ListFundingSourcesRequest$zodSchema } from "../../models/operations/listfundingsources.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListFundingSourcesRequest$zodSchema,
};

export const tool$accounts_fundingSources_list: ToolDefinition<typeof args> = {
  name: "accounts-funding-sources-list",
  description: `List funding sources for an account

List funding source for an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accounts_fundingSources_list(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
