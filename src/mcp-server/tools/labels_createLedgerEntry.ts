/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labels_createLedgerEntry } from "../../funcs/labels_createLedgerEntry.js";
import { CreateLabelLedgerEntryRequest$zodSchema } from "../../models/operations/createlabelledgerentry.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateLabelLedgerEntryRequest$zodSchema,
};

export const tool$labels_createLedgerEntry: ToolDefinition<typeof args> = {
  name: "labels-create-ledger-entry",
  description: `Create a label ledger entry

Create a new entry on a Label Ledger. The amount tied to a Label cannot go negative, therefore if the amount of the label ledger entry exceeds the current amount tied to a Label then a validation error will be returned.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labels_createLedgerEntry(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
