/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kba_verify } from "../../funcs/kba_verify.js";
import { VerifyRequest$zodSchema } from "../../models/operations/verify.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VerifyRequest$zodSchema,
};

export const tool$kba_verify: ToolDefinition<typeof args> = {
  name: "kba-verify",
  description: `Verify KBA Questions

Verify KBA Questions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kba_verify(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
