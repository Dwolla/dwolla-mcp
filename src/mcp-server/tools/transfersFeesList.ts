/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfersFeesList } from "../../funcs/transfersFeesList.js";
import { ListTransferFeesRequest$zodSchema } from "../../models/listtransferfeesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTransferFeesRequest$zodSchema,
};

export const tool$transfersFeesList: ToolDefinition<typeof args> = {
  name: "transfers-fees-list",
  description: `List fees for a transfer

Retrieve detailed fee information for a specific transfer by its unique identifier. Returns the total number of fees and individual fee transaction details including amounts, status, and links to source and destination accounts.`,
  scopes: ["read"],
  annotations: {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfersFeesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
