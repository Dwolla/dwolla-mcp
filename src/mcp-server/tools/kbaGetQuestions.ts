/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kbaGetQuestions } from "../../funcs/kbaGetQuestions.js";
import { GetKbaQuestionsRequest$zodSchema } from "../../models/getkbaquestionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetKbaQuestionsRequest$zodSchema,
};

export const tool$kbaGetQuestions: ToolDefinition<typeof args> = {
  name: "kba-get-questions",
  description: `Retrieve KBA Questions

Returns the KBA questions for a specific KBA session. The questions are used to verify the customer's identity during the KBA process.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kbaGetQuestions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
