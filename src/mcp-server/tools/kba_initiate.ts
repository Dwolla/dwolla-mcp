/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kba_initiate } from "../../funcs/kba_initiate.js";
import { InitiateKbaForCustomerRequest$zodSchema } from "../../models/operations/initiatekbaforcustomer.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: InitiateKbaForCustomerRequest$zodSchema,
};

export const tool$kba_initiate: ToolDefinition<typeof args> = {
  name: "kba-initiate",
  description: `Initiate a KBA session

Initiate a KBA session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kba_initiate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
