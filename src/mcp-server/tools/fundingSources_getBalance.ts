/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSources_getBalance } from "../../funcs/fundingSources_getBalance.js";
import { GetFundingSourceBalanceRequest$zodSchema } from "../../models/operations/getfundingsourcebalance.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFundingSourceBalanceRequest$zodSchema,
};

export const tool$fundingSources_getBalance: ToolDefinition<typeof args> = {
  name: "funding-sources-get-balance",
  description: `Retrieve funding source balance

Retrieve funding source balance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSources_getBalance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
