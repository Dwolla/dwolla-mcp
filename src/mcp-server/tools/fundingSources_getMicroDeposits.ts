/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSources_getMicroDeposits } from "../../funcs/fundingSources_getMicroDeposits.js";
import { GetMicroDepositsRequest$zodSchema } from "../../models/operations/getmicrodeposits.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMicroDepositsRequest$zodSchema,
};

export const tool$fundingSources_getMicroDeposits: ToolDefinition<typeof args> =
  {
    name: "funding-sources-get-micro-deposits",
    description: `Retrieve micro-deposits details

Retrieve micro-deposits details`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await fundingSources_getMicroDeposits(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
