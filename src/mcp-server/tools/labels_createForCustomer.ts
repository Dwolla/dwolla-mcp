/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labels_createForCustomer } from "../../funcs/labels_createForCustomer.js";
import { CreateCustomerLabelRequest$zodSchema } from "../../models/operations/createcustomerlabel.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateCustomerLabelRequest$zodSchema,
};

export const tool$labels_createForCustomer: ToolDefinition<typeof args> = {
  name: "labels-create-for-customer",
  description: `Create a label for a customer

Create a label for a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labels_createForCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
