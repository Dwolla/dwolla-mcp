/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customers_documents_list } from "../../funcs/customers_documents_list.js";
import { ListCustomerDocumentsRequest$zodSchema } from "../../models/operations/listcustomerdocuments.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomerDocumentsRequest$zodSchema,
};

export const tool$customers_documents_list: ToolDefinition<typeof args> = {
  name: "customers-documents-list",
  description: `List documents for customer

List documents for customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customers_documents_list(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
