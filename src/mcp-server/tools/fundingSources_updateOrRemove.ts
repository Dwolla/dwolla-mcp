/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSources_updateOrRemove } from "../../funcs/fundingSources_updateOrRemove.js";
import { UpdateOrRemoveFundingSourceRequest$zodSchema } from "../../models/operations/updateorremovefundingsource.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateOrRemoveFundingSourceRequest$zodSchema,
};

export const tool$fundingSources_updateOrRemove: ToolDefinition<typeof args> = {
  name: "funding-sources-update-or-remove",
  description: `Update or remove a funding source

Update or remove a funding source`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSources_updateOrRemove(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
