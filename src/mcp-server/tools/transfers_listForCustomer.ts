/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfers_listForCustomer } from "../../funcs/transfers_listForCustomer.js";
import { ListCustomerTransfersRequest$zodSchema } from "../../models/operations/listcustomertransfers.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomerTransfersRequest$zodSchema,
};

export const tool$transfers_listForCustomer: ToolDefinition<typeof args> = {
  name: "transfers-list-for-customer",
  description: `List and search transfers for a customer

List and search transfers for a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfers_listForCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
