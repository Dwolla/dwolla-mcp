/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accounts_massPayments_list } from "../../funcs/accounts_massPayments_list.js";
import { ListMassPaymentsRequest$zodSchema } from "../../models/operations/listmasspayments.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListMassPaymentsRequest$zodSchema,
};

export const tool$accounts_massPayments_list: ToolDefinition<typeof args> = {
  name: "accounts-mass-payments-list",
  description: `List mass payments for an account

List mass payments for an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accounts_massPayments_list(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
