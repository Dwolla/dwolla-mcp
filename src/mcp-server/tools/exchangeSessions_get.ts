/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { exchangeSessions_get } from "../../funcs/exchangeSessions_get.js";
import { RetrieveCustomerExchangeSessionRequest$zodSchema } from "../../models/operations/retrievecustomerexchangesession.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RetrieveCustomerExchangeSessionRequest$zodSchema,
};

export const tool$exchangeSessions_get: ToolDefinition<typeof args> = {
  name: "exchange-sessions-get",
  description: `Retrieve exchange session

Retrieve details of an exchange session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await exchangeSessions_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
