/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPayments_getItem } from "../../funcs/massPayments_getItem.js";
import { GetMassPaymentItemRequest$zodSchema } from "../../models/operations/getmasspaymentitem.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMassPaymentItemRequest$zodSchema,
};

export const tool$massPayments_getItem: ToolDefinition<typeof args> = {
  name: "mass-payments-get-item",
  description: `Retrieve mass payment item

Retrieve mass payment item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPayments_getItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
