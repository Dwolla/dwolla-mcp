/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPayments_listForCustomer } from "../../funcs/massPayments_listForCustomer.js";
import { ListCustomerMassPaymentsRequest$zodSchema } from "../../models/operations/listcustomermasspayments.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomerMassPaymentsRequest$zodSchema,
};

export const tool$massPayments_listForCustomer: ToolDefinition<typeof args> = {
  name: "mass-payments-list-for-customer",
  description: `List mass payments for customer

List mass payments for customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPayments_listForCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
