/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labels_getReallocation } from "../../funcs/labels_getReallocation.js";
import { RetrieveLabelReallocationRequest$zodSchema } from "../../models/operations/retrievelabelreallocation.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RetrieveLabelReallocationRequest$zodSchema,
};

export const tool$labels_getReallocation: ToolDefinition<typeof args> = {
  name: "labels-get-reallocation",
  description: `Retrieve a label reallocation

Retrieve a label reallocation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labels_getReallocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
