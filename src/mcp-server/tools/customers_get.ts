/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customers_get } from "../../funcs/customers_get.js";
import { GetCustomerRequest$zodSchema } from "../../models/operations/getcustomer.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCustomerRequest$zodSchema,
};

export const tool$customers_get: ToolDefinition<typeof args> = {
  name: "customers-get",
  description: `Retrieve a customer

Retrieve details for a single customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customers_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
