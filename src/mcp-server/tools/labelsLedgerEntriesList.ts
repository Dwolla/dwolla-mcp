/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labelsLedgerEntriesList } from "../../funcs/labelsLedgerEntriesList.js";
import { ListLabelLedgerEntriesRequest$zodSchema } from "../../models/listlabelledgerentriesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListLabelLedgerEntriesRequest$zodSchema,
};

export const tool$labelsLedgerEntriesList: ToolDefinition<typeof args> = {
  name: "labels-ledger-entries-list",
  description: `List label ledger entries

Returns all ledger entries for a specific Label, sorted by creation date (newest first). Supports pagination with limit and offset parameters. Each ledger entry includes its amount, currency, and creation timestamp.`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labelsLedgerEntriesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
