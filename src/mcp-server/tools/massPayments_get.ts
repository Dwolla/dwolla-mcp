/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { massPayments_get } from "../../funcs/massPayments_get.js";
import { GetMassPaymentRequest$zodSchema } from "../../models/operations/getmasspayment.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMassPaymentRequest$zodSchema,
};

export const tool$massPayments_get: ToolDefinition<typeof args> = {
  name: "mass-payments-get",
  description: `Retrieve a mass payment

Retrieve a mass payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await massPayments_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
