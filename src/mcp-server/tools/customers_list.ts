/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customers_list } from "../../funcs/customers_list.js";
import { ListAndSearchCustomersRequest$zodSchema } from "../../models/operations/listandsearchcustomers.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAndSearchCustomersRequest$zodSchema.optional(),
};

export const tool$customers_list: ToolDefinition<typeof args> = {
  name: "customers-list",
  description: `List and search customers

List and search customers allowing you to filter by email and status, as well as search on key fields such as firstName, lastName, and businessName.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customers_list(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
