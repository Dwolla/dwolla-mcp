/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookSubscriptions_create } from "../../funcs/webhookSubscriptions_create.js";
import { CreateWebhookSubscriptionRequest$zodSchema } from "../../models/operations/createwebhooksubscription.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateWebhookSubscriptionRequest$zodSchema,
};

export const tool$webhookSubscriptions_create: ToolDefinition<typeof args> = {
  name: "webhook-subscriptions-create",
  description: `Create a webhook subscription

Create a webhook subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhookSubscriptions_create(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
