/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { businessClassifications_list } from "../../funcs/businessClassifications_list.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$businessClassifications_list: ToolDefinition = {
  name: "business-classifications-list",
  description: `List business classifications

Retrieve an _embedded list of business classifications that contains an _embedded list of industry classifications.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await businessClassifications_list(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
