/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labels_listLedgerEntries } from "../../funcs/labels_listLedgerEntries.js";
import { ListLabelLedgerEntriesRequest$zodSchema } from "../../models/operations/listlabelledgerentries.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListLabelLedgerEntriesRequest$zodSchema,
};

export const tool$labels_listLedgerEntries: ToolDefinition<typeof args> = {
  name: "labels-list-ledger-entries",
  description: `List label ledger entries

List label ledger entries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labels_listLedgerEntries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
