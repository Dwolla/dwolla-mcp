/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customers_beneficialOwners_create } from "../../funcs/customers_beneficialOwners_create.js";
import { CreateBeneficialOwnerForCustomerRequest$zodSchema } from "../../models/operations/createbeneficialownerforcustomer.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateBeneficialOwnerForCustomerRequest$zodSchema,
};

export const tool$customers_beneficialOwners_create: ToolDefinition<
  typeof args
> = {
  name: "customers-beneficial-owners-create",
  description: `Create beneficial owner

Create beneficial owner which is a natural person who, directly or indirectly, owns 25% or more of the equity interests of the company.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customers_beneficialOwners_create(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
