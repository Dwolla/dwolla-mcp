/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customers_create } from "../../funcs/customers_create.js";
import { CreateCustomerRequest$zodSchema } from "../../models/operations/createcustomer.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateCustomerRequest$zodSchema,
};

export const tool$customers_create: ToolDefinition<typeof args> = {
  name: "customers-create",
  description: `Create a customer

Create an unverified customer, verified customer, or receive-only user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customers_create(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
