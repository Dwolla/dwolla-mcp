/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transfers_get } from "../../funcs/transfers_get.js";
import { GetTransferRequest$zodSchema } from "../../models/operations/gettransfer.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTransferRequest$zodSchema,
};

export const tool$transfers_get: ToolDefinition<typeof args> = {
  name: "transfers-get",
  description: `Retrieve a transfer

Retrieve a transfer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transfers_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
