/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { labels_listForCustomer } from "../../funcs/labels_listForCustomer.js";
import { ListCustomerLabelsRequest$zodSchema } from "../../models/operations/listcustomerlabels.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListCustomerLabelsRequest$zodSchema,
};

export const tool$labels_listForCustomer: ToolDefinition<typeof args> = {
  name: "labels-list-for-customer",
  description: `List labels for a customer

List labels for a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await labels_listForCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
