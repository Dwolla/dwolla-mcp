/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooks_get } from "../../funcs/webhooks_get.js";
import { GetWebhookRequest$zodSchema } from "../../models/operations/getwebhook.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWebhookRequest$zodSchema,
};

export const tool$webhooks_get: ToolDefinition<typeof args> = {
  name: "webhooks-get",
  description: `Retrieve a webhook

Retrieve a webhook`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooks_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
