/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookSubscriptions_get } from "../../funcs/webhookSubscriptions_get.js";
import { GetWebhookSubscriptionRequest$zodSchema } from "../../models/operations/getwebhooksubscription.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWebhookSubscriptionRequest$zodSchema,
};

export const tool$webhookSubscriptions_get: ToolDefinition<typeof args> = {
  name: "webhook-subscriptions-get",
  description: `Retrieve a webhook subscription

Retrieve a webhook subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhookSubscriptions_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
