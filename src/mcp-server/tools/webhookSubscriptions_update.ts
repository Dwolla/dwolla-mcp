/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookSubscriptions_update } from "../../funcs/webhookSubscriptions_update.js";
import { UpdateWebhookSubscriptionRequest$zodSchema } from "../../models/operations/updatewebhooksubscription.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateWebhookSubscriptionRequest$zodSchema,
};

export const tool$webhookSubscriptions_update: ToolDefinition<typeof args> = {
  name: "webhook-subscriptions-update",
  description: `Update a webhook subscription

Update a webhook subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhookSubscriptions_update(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
