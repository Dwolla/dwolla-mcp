/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fundingSources_get } from "../../funcs/fundingSources_get.js";
import { GetFundingSourceRequest$zodSchema } from "../../models/operations/getfundingsource.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFundingSourceRequest$zodSchema,
};

export const tool$fundingSources_get: ToolDefinition<typeof args> = {
  name: "funding-sources-get",
  description: `Retrieve a funding source

Retrieve a funding source`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fundingSources_get(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
