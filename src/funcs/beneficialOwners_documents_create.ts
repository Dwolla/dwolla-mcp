/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { DwollaMcpCore } from "../core.js";
import { appendForm, encodeSimple } from "../lib/encodings.js";
import {
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  CreateBeneficialOwnerDocumentRequest,
  CreateBeneficialOwnerDocumentRequest$zodSchema,
  CreateBeneficialOwnerDocumentResponse,
  CreateBeneficialOwnerDocumentResponse$zodSchema,
} from "../models/operations/createbeneficialownerdocument.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Create a document for beneficial owner
 *
 * @remarks
 * Create a document for beneficial owner
 */
export function beneficialOwners_documents_create(
  client$: DwollaMcpCore,
  request: CreateBeneficialOwnerDocumentRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    CreateBeneficialOwnerDocumentResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: DwollaMcpCore,
  request: CreateBeneficialOwnerDocumentRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      CreateBeneficialOwnerDocumentResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => CreateBeneficialOwnerDocumentRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = new FormData();
  if (payload$.RequestBody.documentType !== undefined) {
    appendForm(body$, "documentType", payload$.RequestBody.documentType);
  }
  if (payload$.RequestBody.file !== undefined) {
    if (isBlobLike(payload$.RequestBody.file)) {
      appendForm(body$, "file", payload$.RequestBody.file);
    } else if (isReadableStream(payload$.RequestBody.file.content)) {
      const buffer = await readableStreamToArrayBuffer(
        payload$.RequestBody.file.content,
      );
      const contentType =
        getContentTypeFromFileName(payload$.RequestBody.file.fileName)
        || "application/octet-stream";
      const blob = new Blob([buffer], { type: contentType });
      appendForm(body$, "file", blob, payload$.RequestBody.file.fileName);
    } else {
      const contentType =
        getContentTypeFromFileName(payload$.RequestBody.file.fileName)
        || "application/octet-stream";
      appendForm(
        body$,
        "file",
        new Blob([payload$.RequestBody.file.content], { type: contentType }),
        payload$.RequestBody.file.fileName,
      );
    }
  }

  const pathParams$ = {
    id: encodeSimple("id", payload$.id, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc("/beneficial-owners/{id}/documents")(
    pathParams$,
  );

  const headers$ = new Headers(compactMap({
    Accept: "application/vnd.dwolla.v1.hal+json",
  }));
  const securityInput = await extractSecurity(client$._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "createBeneficialOwnerDocument",
    oAuth2Scopes: [],
    resolvedSecurity: requestSecurity,
    securitySource: client$._options.security,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    CreateBeneficialOwnerDocumentResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.nil(201, CreateBeneficialOwnerDocumentResponse$zodSchema, { hdrs: true }),
    M.json(400, CreateBeneficialOwnerDocumentResponse$zodSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
      key: "oneOf",
    }),
    M.json(403, CreateBeneficialOwnerDocumentResponse$zodSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
      key: "ForbiddenError",
    }),
    M.json(404, CreateBeneficialOwnerDocumentResponse$zodSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
      key: "NotFoundError",
    }),
    M.json(413, CreateBeneficialOwnerDocumentResponse$zodSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
      key: "object",
    }),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
