/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { DwollaMcpCore } from "../core.js";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  CreateReAuthExchangeSessionRequest,
  CreateReAuthExchangeSessionRequest$zodSchema,
  CreateReAuthExchangeSessionResponse,
  CreateReAuthExchangeSessionResponse$zodSchema,
} from "../models/operations/createreauthexchangesession.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Create re-authentication exchange session
 *
 * @remarks
 * Create a new re-authentication exchange session.
 *
 * Request body requirements vary by exchange type:
 * - For Visa: Request body with redirect URL is required
 * - For Plaid Web: No request body is required
 * - For Plaid Mobile: Request body with redirect URL is required
 *
 * The redirect URL will be validated against previously configured URLs stored in Dwolla.
 * After completing the re-authorization process, the user will be redirected back to your application using this URL.
 */
export function exchanges_createReAuth(
  client$: DwollaMcpCore,
  request: CreateReAuthExchangeSessionRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    CreateReAuthExchangeSessionResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: DwollaMcpCore,
  request: CreateReAuthExchangeSessionRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      CreateReAuthExchangeSessionResponse,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => CreateReAuthExchangeSessionRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = encodeJSON("body", payload$.CreateReAuthExchangeSession, {
    explode: true,
  });

  const pathParams$ = {
    id: encodeSimple("id", payload$.id, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path$ = pathToFunc("/exchanges/{id}/exchange-sessions")(
    pathParams$,
  );

  const headers$ = new Headers(compactMap({
    "Content-Type": "application/vnd.dwolla.v1.hal+json",
    Accept: "application/vnd.dwolla.v1.hal+json",
  }));
  const securityInput = await extractSecurity(client$._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "createReAuthExchangeSession",
    oAuth2Scopes: [],
    resolvedSecurity: requestSecurity,
    securitySource: client$._options.security,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  const response = doResult.value;
  const responseFields$ = {
    HttpMeta: { Response: response, Request: req$ },
  };

  const [result$] = await M.match<
    CreateReAuthExchangeSessionResponse,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.nil(201, CreateReAuthExchangeSessionResponse$zodSchema, { hdrs: true }),
    M.json(400, CreateReAuthExchangeSessionResponse$zodSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
      key: "400_application/vnd.dwolla.v1.hal+json_object",
    }),
    M.json(403, CreateReAuthExchangeSessionResponse$zodSchema, {
      ctype: "application/vnd.dwolla.v1.hal+json",
      key: "403_application/vnd.dwolla.v1.hal+json_object",
    }),
  )(response, req$, { extraFields: responseFields$ });

  return [result$, { status: "complete", request: req$, response }];
}
